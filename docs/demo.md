# Seeded demo

INC-1042 begins with healthy database metrics. Deployment v2.8.1 introduces three sequential queries, followed by pool saturation, latency, and checkout failures. Payment provider degradation remains plausible but is contradicted by status and timing evidence. Rollback reverses the correlated signals.

Start at Overview, open the incident, click timeline events for provenance, compare the three hypotheses, inspect deterministic scoring, switch to Evidence graph and Agent replay, and review Evaluation. The generated architecture artifact is stored at `public/demo/checkout-architecture.png`.
