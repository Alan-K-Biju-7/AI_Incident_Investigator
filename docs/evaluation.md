# Evaluation

Synthetic incidents include machine-readable ground truth. Evaluation compares predicted root cause, cited evidence, normalized timeline, top-k hypothesis position, predicted confidence versus observed correctness, unnecessary tool calls, duration, and unsupported claims. Dashboard values are represented as assertion counts, not model opinion. The generator accepts service, failure, and seed arguments for reproducible scenarios.
